# Week 1-3 scope: ingestion, validation, EDA only.
# Baseline model / Voting Classifier / FastAPI / React dependencies will be
# added to backend/requirements.txt and frontend/package.json in later weeks.
pandas==2.2.2
numpy==1.26.4
matplotlib==3.8.4
seaborn==0.13.2
scikit-learn==1.4.2
pytest==8.2.0
